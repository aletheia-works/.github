# Dependabot configuration for the aletheia-works/.github repo.
#
# Mirrors vivarium/.github/dependabot.yml so caller repos and this repo
# track the same cadence and label taxonomy. The primary job here is to
# keep third-party action SHA pins current — in particular the ones used
# inside the reusable terraform-plan/apply workflows.

version: 2
updates:
  # ─── GitHub Actions ──────────────────────────
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
      time: "03:00"
      timezone: "Asia/Tokyo"
    labels:
      - "type: chore"
      - "scope: ci"
      - "ai: generated"
    commit-message:
      prefix: "chore(actions)"
    # Skip non-security major-version updates — major bumps need targeted
    # human/AI review rather than automated merging.
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
    # Roll every GitHub Actions update into a single daily PR
    # instead of one per action; reviewing the SHA-pinned bumps
    # together cuts merge overhead substantially.
    groups:
      github-actions:
        patterns:
          - "*"
    open-pull-requests-limit: 5

  # ─── OpenTofu providers ──────────────────────
  - package-ecosystem: "terraform"
    directories:
      - "/infra/dotgithub"
      - "/infra/github-org"
    schedule:
      interval: "daily"
      time: "03:00"
      timezone: "Asia/Tokyo"
    labels:
      - "type: chore"
      - "scope: infra"
      - "ai: generated"
    commit-message:
      prefix: "chore(infra)"
    groups:
      terraform-providers:
        patterns:
          - "*"
    open-pull-requests-limit: 3
